api_server: "http://127.0.0.1:8086"
admin_server: "http://127.0.0.1:8080"
agent_id: "agent-001"
batch_size: 50
flush_seconds: 5
host_log_root: ""          # Docker 部署时设为 /host，裸机部署留空
hostname: ""               # Docker 部署时手动指定宿主机 hostname，裸机留空自动获取
