# 使用预编译前端静态资源，不依赖 Node.js 源码
FROM docker.m.daocloud.io/library/nginx:alpine

# 复制预构建的前端静态资源
COPY release/web-dist /usr/share/nginx/html

# 复制 Nginx 配置
COPY deploy/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
